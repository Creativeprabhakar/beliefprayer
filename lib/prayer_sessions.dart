import 'package:flutter/material.dart';

class PrayerSessions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Prayer Sessions')),
      body: Center(child: Text('List of Prayer Sessions')),
    );
  }
}
